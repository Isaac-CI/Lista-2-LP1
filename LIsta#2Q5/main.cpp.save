#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main()
{
    string s, str;
    cin >> s;
    str = s + "_bkp";
    const char* aux = s[0];
    const char auxiliar = str[0];
    cout << aux << "\n" << auxiliar
    ifstream stream(aux, ios::in);
    ofstream fluxo(auxiliar, ios::out);
    return 0;
}
